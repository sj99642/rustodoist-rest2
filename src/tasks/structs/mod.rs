/// Defines the `Task` struct and associated functions.
pub mod task;

/// Defines the `Due` struct, which represents the due date/time of a task.
pub mod due;

/// Defines the `Duration` struct, which represents the duration of a task.
pub mod duration;

/// Defines the `NewTask` struct and its `upload()` function.
pub mod new_task;
